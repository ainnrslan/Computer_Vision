# Computer_Vision
A simple CPU-based image classification web application built using PyTorch and Streamlit that allows users to upload images and view the top-5 predicted classes with their probability scores using a pre-trained ResNet18 model.
